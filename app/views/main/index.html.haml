#index-wrap
  = link_to "Logout", destroy_user_session_path, :method => :delete
  %h1 Users
  %ul
    - @users.each do |user|
      %li
        = user.email
        |
        = link_to "Add friend", "#", class: "add-friend"
